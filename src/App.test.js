import React from 'react';
import { render } from '@testing-library/react';
import App from './App';
import TestRenderer from 'react-test-renderer';s

test('renders learn react link', () => {
  expect(1+2).toBe(3);
});
